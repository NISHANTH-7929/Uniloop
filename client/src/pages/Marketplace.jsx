import React from "react";

// Using plain Bootstrap container instead of react-bootstrap

const Marketplace = () => {
    return (
        <div className="container mt-5">
            <h1>Marketplace</h1>
            <p>Buy and sell items here.</p>
        </div>
    );
};

export default Marketplace;
