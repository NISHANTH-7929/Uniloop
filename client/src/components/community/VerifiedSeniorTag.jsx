import React from "react";

const VerifiedSeniorTag = () => (
    <span style={{
        background: "rgba(255,215,0,0.15)", border: "1px solid rgba(255,215,0,0.35)",
        color: "#ffd700", padding: "2px 8px", borderRadius: "10px",
        fontSize: "0.72rem", fontWeight: "bold",
        display: "inline-flex", alignItems: "center", gap: "4px",
    }}>
        🎓 Senior
    </span>
);

export default VerifiedSeniorTag;
